name: terraform-apply
on:
  workflow_dispatch: {}   # manual only, never auto
jobs:
  apply:
    if: github.ref == 'refs/heads/main'   # only on main
    runs-on: ubuntu-latest
    steps:
      - run: echo "Apply is disabled until real AWS creds are configured."
        shell: bash
